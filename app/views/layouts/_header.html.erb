<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to t(:home), root_path %></li>
        <ul class="nav navbar-nav navbar-right">
        <% if cluster_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "All Clusters", clusters_path %></li>
              <li><%= link_to "Profile", current_cluster %></li>
              <li><%= link_to "Settings", edit_cluster_registration_path %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", destroy_cluster_session_path, method: "delete" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Cluster <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "All Clusters", clusters_path %></li>
              <li><%= link_to "Cluster Log in", new_cluster_session_path %></li>
            </ul>
          </li>
        <% end %>
      </ul>
      </ul>
    </nav>
  </div>
</header>